{
  "name": "react-calendar",
  "version": "1.0.0",
  "description": "Naive Calendar using React",
  "main": "index.js",
  "repository": "https://github.com/soyjavi/react-calendar",
  "author": "JΛVI <hello@soyjavi.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "setup": "rm -rf node_modules && rm -rf yarn.lock && yarn install --force",
    "analyze": "source-map-explorer 'build/static/js/*.js'",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "lint": "eslint --cache --fix --format codeframe --ext .jsx,.js src",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@soyjavi/locale": "^1.0.12",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.1"
  },
  "devDependencies": {
    "@soyjavi/eslint": "^0.1.9",
    "@soyjavi/testing-react": "^0.1.5",
    "react-scripts": "5.0.0",
    "web-vitals": "^2.1.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
